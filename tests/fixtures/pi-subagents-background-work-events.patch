diff --git a/src/api/background-work.ts b/src/api/background-work.ts
index baf4fad..0a4f5f5 100644
--- a/src/api/background-work.ts
+++ b/src/api/background-work.ts
@@ -1,5 +1,26 @@
+import { randomUUID } from "node:crypto";
+
 export const BACKGROUND_WORK_PROTOCOL_VERSION = 1;
 export const BACKGROUND_WORK_REGISTRY_KEY = "pi-subagents.background-work.v1";
+export const BACKGROUND_WORK_REGISTER_EVENT = "pi-subagents:background-work:v1:register";
+export const BACKGROUND_WORK_UNREGISTER_EVENT = "pi-subagents:background-work:v1:unregister";
+
+export interface BackgroundWorkEventBus {
+	emit(channel: string, data: unknown): void;
+	on(channel: string, handler: (data: unknown) => void): () => void;
+}
+
+export interface BackgroundWorkRegistrationPayload {
+	version: typeof BACKGROUND_WORK_PROTOCOL_VERSION;
+	registrationId: string;
+	provider: BackgroundWorkProvider;
+	acknowledge(result: { ok: true } | { ok: false; error: string }): void;
+}
+
+export interface BackgroundWorkUnregisterPayload {
+	version: typeof BACKGROUND_WORK_PROTOCOL_VERSION;
+	registrationId: string;
+}
 
 const MAX_PROVIDER_NAME_LENGTH = 128;
 const MAX_PROVIDERS = 100;
@@ -34,6 +55,15 @@ export interface BackgroundWorkSnapshot {
 	items: readonly RegisteredBackgroundWorkItem[];
 }
 
+export interface BackgroundWorkService {
+	snapshot(sessionId: string, nowMs?: number): BackgroundWorkSnapshot;
+	wakeChannels(): readonly string[];
+}
+
+export interface BackgroundWorkEventBridge extends BackgroundWorkService {
+	dispose(): void;
+}
+
 interface BackgroundWorkRegistry {
 	version: typeof BACKGROUND_WORK_PROTOCOL_VERSION;
 	providers: Map<string, BackgroundWorkProvider>;
@@ -112,6 +142,108 @@ function validateItem(provider: string, value: unknown, index: number): Backgrou
 	};
 }
 
+export function registerBackgroundWorkProviderViaEvents(
+	bus: BackgroundWorkEventBus,
+	provider: BackgroundWorkProvider,
+): () => void {
+	const validated = validateProvider(provider);
+	const registrationId = randomUUID();
+	let acknowledgement: { ok: true } | { ok: false; error: string } | undefined;
+	bus.emit(BACKGROUND_WORK_REGISTER_EVENT, {
+		version: BACKGROUND_WORK_PROTOCOL_VERSION,
+		registrationId,
+		provider: validated,
+		acknowledge(result: { ok: true } | { ok: false; error: string }): void {
+			acknowledgement = result;
+		},
+	} satisfies BackgroundWorkRegistrationPayload);
+	if (acknowledgement === undefined) {
+		throw new Error(`Background-work provider '${validated.name}' has no background-work event bridge.`);
+	}
+	if (!acknowledgement.ok) {
+		throw new Error(`Background-work provider '${validated.name}' registration was rejected: ${acknowledgement.error}`);
+	}
+	let disposed = false;
+	return () => {
+		if (disposed) return;
+		disposed = true;
+		bus.emit(BACKGROUND_WORK_UNREGISTER_EVENT, {
+			version: BACKGROUND_WORK_PROTOCOL_VERSION,
+			registrationId,
+		} satisfies BackgroundWorkUnregisterPayload);
+	};
+}
+
+export function installBackgroundWorkEventBridge(bus: BackgroundWorkEventBus): BackgroundWorkEventBridge {
+	type Registration = { name: string; token: symbol };
+	type ProviderEntry = { provider: BackgroundWorkProvider; token: symbol };
+	const registrations = new Map<string, Registration>();
+	const providers = new Map<string, ProviderEntry>();
+	const listProviders = (): readonly BackgroundWorkProvider[] => {
+		if (providers.size > MAX_PROVIDERS) throw new Error(`Background-work registry contains more than ${MAX_PROVIDERS} providers.`);
+		const listed: BackgroundWorkProvider[] = [];
+		for (const [name, entry] of providers) {
+			const provider = validateProvider(entry.provider);
+			if (name !== provider.name) throw new Error(`Background-work registry key '${name}' does not match provider name '${provider.name}'.`);
+			listed.push(provider);
+		}
+		return listed;
+	};
+	const removeRegistration = (registrationId: string): void => {
+		const registration = registrations.get(registrationId);
+		if (!registration) return;
+		registrations.delete(registrationId);
+		if (providers.get(registration.name)?.token === registration.token) providers.delete(registration.name);
+	};
+	const unsubscribeRegister = bus.on(BACKGROUND_WORK_REGISTER_EVENT, (data) => {
+		if (!data || typeof data !== "object" || Array.isArray(data)) return;
+		const payload = data as Partial<BackgroundWorkRegistrationPayload>;
+		if (typeof payload.acknowledge !== "function") return;
+		try {
+			if (payload.version !== BACKGROUND_WORK_PROTOCOL_VERSION) throw new Error("Unsupported background-work protocol version.");
+			const registrationId = validateString(payload.registrationId, "Background-work registrationId", 256);
+			const provider = validateProvider(payload.provider);
+			if (!providers.has(provider.name) && providers.size >= MAX_PROVIDERS) {
+				throw new Error(`Background-work registry supports at most ${MAX_PROVIDERS} providers.`);
+			}
+			removeRegistration(registrationId);
+			const token = Symbol(provider.name);
+			registrations.set(registrationId, { name: provider.name, token });
+			providers.set(provider.name, { provider, token });
+			payload.acknowledge({ ok: true });
+		} catch (error) {
+			payload.acknowledge({ ok: false, error: error instanceof Error ? error.message : String(error) });
+		}
+	});
+	const unsubscribeUnregister = bus.on(BACKGROUND_WORK_UNREGISTER_EVENT, (data) => {
+		if (!data || typeof data !== "object" || Array.isArray(data)) return;
+		const payload = data as Partial<BackgroundWorkUnregisterPayload>;
+		if (payload.version !== BACKGROUND_WORK_PROTOCOL_VERSION) return;
+		try {
+			removeRegistration(validateString(payload.registrationId, "Background-work registrationId", 256));
+		} catch {
+			// Ignore malformed unregister events.
+		}
+	});
+	let disposed = false;
+	return {
+		snapshot(sessionId, nowMs = Date.now()) {
+			return snapshotProviders(listProviders(), sessionId, nowMs);
+		},
+		wakeChannels() {
+			return wakeChannelsForProviders(listProviders());
+		},
+		dispose() {
+			if (disposed) return;
+			disposed = true;
+			unsubscribeRegister();
+			unsubscribeUnregister();
+			registrations.clear();
+			providers.clear();
+		},
+	};
+}
+
 /**
  * Register or replace one process-local background-work provider. The returned
  * disposer only removes this exact registration, so an old extension reload
@@ -141,19 +273,25 @@ export function listBackgroundWorkProviders(): readonly BackgroundWorkProvider[]
 	return providers;
 }
 
-/** Read validated provider wake channels without reconciling or listing work. */
-export function listBackgroundWorkWakeChannels(): readonly string[] {
+function wakeChannelsForProviders(providers: readonly BackgroundWorkProvider[]): readonly string[] {
 	const channels = new Set<string>();
-	for (const provider of listBackgroundWorkProviders()) {
+	for (const provider of providers) {
 		for (const channel of provider.wakeChannels ?? []) channels.add(channel);
 	}
 	return [...channels];
 }
 
-/** Reconcile and snapshot active provider work owned by one exact Pi session. */
-export function snapshotBackgroundWork(sessionId: string, nowMs = Date.now()): BackgroundWorkSnapshot {
+/** Read validated provider wake channels without reconciling or listing work. */
+export function listBackgroundWorkWakeChannels(): readonly string[] {
+	return wakeChannelsForProviders(listBackgroundWorkProviders());
+}
+
+function snapshotProviders(
+	providers: readonly BackgroundWorkProvider[],
+	sessionId: string,
+	nowMs: number,
+): BackgroundWorkSnapshot {
 	validateString(sessionId, "Background-work snapshot sessionId", MAX_SESSION_ID_LENGTH);
-	const providers = listBackgroundWorkProviders();
 	const items: RegisteredBackgroundWorkItem[] = [];
 	const identities = new Set<string>();
 	for (const provider of providers) {
@@ -195,3 +333,8 @@ export function snapshotBackgroundWork(sessionId: string, nowMs = Date.now()): B
 		items,
 	};
 }
+
+/** Reconcile and snapshot active provider work owned by one exact Pi session. */
+export function snapshotBackgroundWork(sessionId: string, nowMs = Date.now()): BackgroundWorkSnapshot {
+	return snapshotProviders(listBackgroundWorkProviders(), sessionId, nowMs);
+}
diff --git a/src/extension/index.ts b/src/extension/index.ts
index 1e1a37d..d2a67f7 100644
--- a/src/extension/index.ts
+++ b/src/extension/index.ts
@@ -20,6 +20,7 @@ import type { AgentToolResult } from "@earendil-works/pi-agent-core";
 import { keyText, type ExtensionAPI, type ExtensionContext, type ToolDefinition } from "@earendil-works/pi-coding-agent";
 import { Box, Container, Spacer, Text, truncateToWidth, visibleWidth, wrapTextWithAnsi, type Component } from "@earendil-works/pi-tui";
 import { discoverAgents } from "../agents/agents.ts";
+import { installBackgroundWorkEventBridge } from "../api/background-work.ts";
 import { ensureAccessibleDir } from "../shared/accessible-dir.ts";
 import { cleanupAllArtifactDirs, cleanupOldArtifacts, getArtifactsDir } from "../shared/artifacts.ts";
 import { resolveCurrentSessionId } from "../shared/session-identity.ts";
@@ -368,6 +369,7 @@ export default function registerSubagentExtension(pi: ExtensionAPI): void {
 			// Best effort cleanup for stale timers from an older reload.
 		}
 	}
+	const backgroundWork = installBackgroundWorkEventBridge(pi.events);
 
 	DIRS.results = ensureAccessibleDir(DIRS.results);
 	DIRS.async = ensureAccessibleDir(DIRS.async);
@@ -481,6 +483,7 @@ export default function registerSubagentExtension(pi: ExtensionAPI): void {
 		scheduledRunManager.stop();
 		supervisorChannel.dispose();
 		waitSubscriptionManager.dispose();
+		backgroundWork.dispose();
 		fleetStatus?.dispose();
 		disposeAsyncJobTracker();
 	};
@@ -639,10 +642,10 @@ export default function registerSubagentExtension(pi: ExtensionAPI): void {
 
 	pi.registerTool(tool);
 
-	registerWaitTool(pi, state, waitToolConfig.enabled, waitSubscriptionManager);
+	registerWaitTool(pi, state, waitToolConfig.enabled, waitSubscriptionManager, backgroundWork);
 
 	pi.on("agent_end", async (_event, ctx) => {
-		if (!ctx.hasUI) await drainOutstandingWork({ state, events: pi.events });
+		if (!ctx.hasUI) await drainOutstandingWork({ state, events: pi.events, backgroundWork });
 		const ownerSessionId = state.currentSessionId;
 		if (!ownerSessionId) return;
 		goalTurnId += 1;
@@ -884,6 +887,7 @@ export default function registerSubagentExtension(pi: ExtensionAPI): void {
 		state.currentSessionId = null;
 		state.parentSessionFile = null;
 		completionNotifier.dispose();
+		backgroundWork.dispose();
 		delete process.env[SUBAGENT_PARENT_SESSION_ENV];
 		for (const unsubscribe of eventUnsubscribes) {
 			try {
diff --git a/src/runs/background/auto-drain.ts b/src/runs/background/auto-drain.ts
index 8d57e49..8ea2d88 100644
--- a/src/runs/background/auto-drain.ts
+++ b/src/runs/background/auto-drain.ts
@@ -9,6 +9,7 @@ export const DEFAULT_AUTO_DRAIN_TIMEOUT_MS = 30 * 60 * 1000;
 export interface AutoDrainDeps {
 	state: SubagentState;
 	events?: WaitEventBus;
+	backgroundWork?: SubagentWaitDeps["backgroundWork"];
 	timeoutMs?: number;
 	now?: () => number;
 	wait?: (
@@ -23,14 +24,14 @@ function resultText(value: AgentToolResult<Details>): string {
 	return value.content.map((part) => part.type === "text" ? part.text : "").join(" ").trim();
 }
 
-function hasOutstandingWork(sessionId: string, nowMs: number): boolean {
+function hasOutstandingWork(sessionId: string, nowMs: number, backgroundWork?: SubagentWaitDeps["backgroundWork"]): boolean {
 	const asyncRuns = listAsyncRuns(DIRS.async, {
 		states: ["queued", "running"],
 		sessionId,
 		resultsDir: DIRS.results,
 		now: () => nowMs,
 	});
-	return asyncRuns.length > 0 || snapshotBackgroundWork(sessionId, nowMs).items.length > 0;
+	return asyncRuns.length > 0 || (backgroundWork?.snapshot(sessionId, nowMs) ?? snapshotBackgroundWork(sessionId, nowMs)).items.length > 0;
 }
 
 /** Drain all work owned by the current headless session, including work added while draining. */
@@ -41,7 +42,7 @@ export async function drainOutstandingWork(deps: AutoDrainDeps): Promise<void> {
 	const timeoutMs = deps.timeoutMs ?? DEFAULT_AUTO_DRAIN_TIMEOUT_MS;
 	if (!Number.isFinite(timeoutMs) || timeoutMs <= 0) throw new Error("Auto-drain timeoutMs must be a positive finite number.");
 	const deadlineAt = now() + timeoutMs;
-	const hasWork = deps.hasWork ?? hasOutstandingWork;
+	const hasWork = deps.hasWork ?? ((sessionId: string, nowMs: number) => hasOutstandingWork(sessionId, nowMs, deps.backgroundWork));
 	const wait = deps.wait ?? waitForSubagents;
 
 	while (hasWork(sessionId, now())) {
@@ -55,6 +56,7 @@ export async function drainOutstandingWork(deps: AutoDrainDeps): Promise<void> {
 			{
 				state: deps.state,
 				events: deps.events,
+				backgroundWork: deps.backgroundWork,
 				now,
 				stopOnAttention: false,
 				failOnFailedRuns: true,
diff --git a/src/runs/background/wait-tool.ts b/src/runs/background/wait-tool.ts
index 126f9c0..df4b630 100644
--- a/src/runs/background/wait-tool.ts
+++ b/src/runs/background/wait-tool.ts
@@ -1,10 +1,16 @@
 import type { ExtensionAPI, ToolDefinition } from "@earendil-works/pi-coding-agent";
 import { SubagentWaitParams } from "../../extension/schemas.ts";
 import type { Details, SubagentState } from "../../shared/types.ts";
-import { resolveWaitToolConfig, waitForSubagents } from "./subagent-wait.ts";
+import { resolveWaitToolConfig, waitForSubagents, type SubagentWaitDeps } from "./subagent-wait.ts";
 import type { WaitSubscriptionManager } from "./wait-subscriptions.ts";
 
-export function registerWaitTool(pi: ExtensionAPI, state: SubagentState, enabled = resolveWaitToolConfig().enabled, subscriptions?: Pick<WaitSubscriptionManager, "arm">): void {
+export function registerWaitTool(
+	pi: ExtensionAPI,
+	state: SubagentState,
+	enabled = resolveWaitToolConfig().enabled,
+	subscriptions?: Pick<WaitSubscriptionManager, "arm">,
+	backgroundWork?: SubagentWaitDeps["backgroundWork"],
+): void {
 	const tool: ToolDefinition<typeof SubagentWaitParams, Details> = {
 		name: "subagent_wait",
 		label: "Subagent Wait",
@@ -26,6 +32,7 @@ Non-blocking subscriptions are visible in subagent status and differ from disabl
 				events: pi.events,
 				enabled,
 				onUpdate,
+				backgroundWork,
 				...(subscriptions && ctx?.hasUI ? { subscribe: (input) => subscriptions.arm(input) } : {}),
 			});
 		},
