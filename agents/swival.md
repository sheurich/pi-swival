---
name: swival
description: Generic swival delegate. No system prompt, no review by default. Toggle features via dispatch-time overrides (selfReviewOverride, reviewerOverride, etc). Uses the model from swival's global config.toml.
noInstructions: true
noMemory: true
files: some
commands: all
---
